{% extends 'base.html.twig' %}

{% block body %}
    <h1>Galery</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galery.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ galery.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('galery_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('galery_edit', { 'id': galery.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
